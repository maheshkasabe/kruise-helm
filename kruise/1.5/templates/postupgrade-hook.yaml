apiVersion: batch/v1
kind: Job
metadata:
   name: postupgrade-hook
   annotations:
       "helm.sh/hook": "post-upgrade"
spec:
  template:
    spec:
      containers:
      - name: health-check-container
        image: maheshkasbe/helm:v2
        command: ["/bin/sh", "-c"]
        args:
        - |
            lua healthcheck.lua
      restartPolicy: Never
      serviceAccountName: default